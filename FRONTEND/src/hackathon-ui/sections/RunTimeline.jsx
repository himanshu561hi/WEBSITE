import React from "react";
import TimelineSection from "./TimelineSection";

/**
 * RunTimeline
 * Alias / wrapper for TimelineSection.
 */
export default function RunTimeline() {
  return <TimelineSection />;
}
